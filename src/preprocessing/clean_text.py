import re
import pandas as pd
import os

def clean_text(text):
    """
    Preprocess text for ML/RL.
    Steps:
    - Lowercase
    - Remove URLs
    - Remove special characters
    - Strip extra spaces
    """

def preprocess_csv(input_file, output_file):
    """
    Read CSV, clean text column, save new CSV.
    """
